# Legacy Supabase Migrations

These files are preserved for reference only and must not be applied with the
current MedTrace mobile schema:

- `001_init.sql`
- `002_registration.sql`

They conflict with the canonical timestamped migrations in `supabase/migrations`
and are intentionally outside the Supabase CLI migration folder.
